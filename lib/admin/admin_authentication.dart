
import 'package:flutter/material.dart';

enum AdminScreenState{
  adminEmailPassword,
  adminLoggedIn,
}

class AdminAuthentication extends StatelessWidget {
  const AdminAuthentication({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}